import React from "react";
// import useSWR from "swr";
// import { getBook } from "../utils/api/models/books";

export const Books = () => {
    // const { data } = useSWR('1', getBook);
    
    return <div>
        <h2>Books</h2>
    </div>
}